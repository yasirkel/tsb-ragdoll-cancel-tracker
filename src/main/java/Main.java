import Entities.RagdollCancelTimer;
import javafx.application.Application;

public class Main {
    public static void main(String[] args) {
        // Run the application with administrator privileges
        Application.launch(RagdollCancelTimer.class);
    }
}